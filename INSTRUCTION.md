## To start build app and start containers need to use command

```
docker-compose up -d
```

## To stop containers and remove them use command

```
docker-compose down
```

## To get acces to the running app use link

http://localhost:8080/
